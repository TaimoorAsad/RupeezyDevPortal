# Nixpacks config — tells Railway to install both Python 3.11 and Node 20
[phases.setup]
nixPkgs = ["python311", "nodejs_20", "nodePackages.npm"]

[phases.build]
cmds = ["bash build.sh"]

[start]
cmd = "gunicorn --worker-class eventlet -w 1 --chdir backend app:app --bind 0.0.0.0:$PORT"
