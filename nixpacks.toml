# Railpack/Nixpacks config — Python only (frontend is pre-built)
[phases.setup]
nixPkgs = ["python311"]

[phases.build]
cmds = ["bash build.sh"]

[start]
cmd = "gunicorn --worker-class eventlet -w 1 --chdir backend app:app --bind 0.0.0.0:$PORT"
